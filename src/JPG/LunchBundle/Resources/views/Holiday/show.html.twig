{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Holidays</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('setup_menu_holidays') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('setup_menu_holidays_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
